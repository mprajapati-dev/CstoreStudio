
---def visual_triage(state: JobState):
---    # Call Local Ollama via LiteLLM
---    try:
---        response = litellm.completion(
---            model=os.getenv("LLM_MODEL", "ollama/llava"), # Recommended vision model
---            api_base=os.getenv("OLLAMA_API_BASE", "http://host.docker.internal:11434"), # Access host's Ollama from Docker
---            messages=[{"role": "user", "content": f"Analyze this media issue: {state['media_url']}"}]
---        )
---        triage_text = response.choices[0].message.content
---        token_usage.labels(model="ollama").inc(getattr(response.usage, 'total_tokens', 0))
---    except Exception as e:
---        triage_text = "Analysis Failed due to error: " + str(e)
---        
---    state["triage_result"] = triage_text
---    return state
---
---
